@model IEnumerable<HotelReservation.Models.Testimonial>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comments)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Userloginid2Navigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Comments)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Userloginid2Navigation.Userloginid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Testimonialid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Testimonialid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Testimonialid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
