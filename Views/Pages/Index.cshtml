@model IEnumerable<HotelReservation.Models.Page>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}
<br />
<br />
<br />
<br />
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pagename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pageimage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pagecontent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pagename)
            </td>
            <td>
                    <img src=@Url.Content("~/Images/"+ item.Pageimage )
                         asp-append-version="true" style="border-radius:15%"
                         width="100" height="100" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pagecontent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pageid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pageid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pageid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
